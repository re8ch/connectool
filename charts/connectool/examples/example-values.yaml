global:
  issuer: https://identity.example.com
  scopes: [openid, profile, email, groups, offline_access]
dex:
  contract: {enabled: true, namespace: identity-system, configMapName: connectool-oidc-clients}
toolhive:
  namespace: toolhive-system
  registry:
    enabled: true
    name: approved
    sources:
      - {name: managed-workloads, kubernetes: {namespaces: [toolhive-system]}}
    release: {enabled: false, version: 1.5.0, sourceNamespace: flux-system, values: {}}
  sessionStorage:
    provider: redis
    address: ""
    passwordSecretRef: {name: "", key: password}
  publications:
    - name: platform-tools
      clientId: platform-tools
      resourceUrl: https://tools.example.com/mcp
      group: platform-tools
      createGroup: true
      workloads:
        - {name: observe, toolConfigRef: observe-tools}
      authorization:
        groupClaim: groups
        policies: ['permit(principal, action, resource);']
codex:
  marketplace:
    name: example-tools
    displayName: Example Tools
    source: https://github.com/example/codex-plugins.git
  plugins:
    - name: platform-tool
      publication: platform-tools
      version: 1.0.0
      category: Productivity
      mcpOwner: true
      skills:
        - path: skills/platform/SKILL.md
          content: |
            ---
            name: platform
            description: Inspect the example platform through its authenticated MCP gateway.
            ---
            # Platform

            Use this skill for read-only platform inspection.
